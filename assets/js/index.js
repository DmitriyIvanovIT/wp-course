const swiperPhotoReport = new Swiper('.photo-report-slider', {
    loop: true,
    autoplay: {
        delay: 5000,
    },
    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
    },
});